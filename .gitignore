.idea
target/
build/
dist/
*.class

### VS Code ###
.vscode/

### Mac OS ###
.DS_Store
.claude/worktrees/

# an assembler run by hand leaves an object file beside the source
*.o

# a go build run by hand leaves the executable beside the module
/go/dtx-write
/go/dtx-package
/go/dtx-blobs

# the C# tree's build outputs
dotnet/bin/
dotnet/obj/

# go/internal/image/data contains its own .gitignore, as YMX's cores/data
# does: the images there are build output and the directory itself is
# committed so that go:embed always has one to read.

__pycache__/
